# GymManager
GymManager is a web application for managing a gym. It allows administrators to handle members, trainers, memberships, and training sessions, while users can register, log in, purchase memberships, and book training sessions.
